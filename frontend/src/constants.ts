// Number of drivers shown in standings table before "Show All" is clicked
export const TOP_STANDINGS_COUNT = 10;

// Default number of drivers shown in the race positions chart selector
export const DEFAULT_DRIVER_LIMIT = 10;

// Standard chart height in pixels
export const CHART_HEIGHT = 600;
