# URL of the backend API server.
# When running locally, this points to the Express server.
# On deployment (e.g. Vercel + Render), set this to your Render service URL.
VITE_API_URL=http://localhost:5000
